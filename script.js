// on mouseclick in navbar send alert
function cityAlert() {
    alert("Loading weather report...");
}

// on mouse click, remove cookie window
function removeCookie(element) {
    element.remove();
}